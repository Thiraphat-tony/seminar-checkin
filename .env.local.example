# Copy this to .env.local and fill with your Supabase project values
# Do NOT commit your .env.local with real secrets to source control.

# Public Supabase URL (required)
NEXT_PUBLIC_SUPABASE_URL=https://your-project-ref.supabase.co

# Public anon key for client-side requests (required for public reads)
NEXT_PUBLIC_SUPABASE_ANON_KEY=public-anon-key-here

# Server-only service role key (use ONLY on server; DO NOT prefix with NEXT_PUBLIC_)
SUPABASE_SERVICE_ROLE_KEY=service-role-key-here

# Creator-only manager reset (hidden /manager page)
MANAGER_CREATOR_PASSPHRASE=change-this-passphrase
MANAGER_ADMIN_EMAIL=admin@example.com
